@{
    ViewData["Title"] = "Index";
}

<h1>Macoratti .net</h1>
<hr />
<div class="jumbotron jumbotron-fluid">
    <div class="container">
        @if (ViewData["Error"] != null)
        {
            <h2 class="display-4">@ViewData["Erro"]</h2>
        }
        else if (ViewData["Resultado"] != null)
        {
            <h2 class="display-4">@ViewData["Resultado"]</h2>
        }
    </div>
</div>